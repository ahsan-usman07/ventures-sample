import { AppScrollNarrowDirective } from './app-scroll-narrow.directive';

describe('AppScrollNarrowDirective', () => {
  it('should create an instance', () => {
    const directive = new AppScrollNarrowDirective();
    expect(directive).toBeTruthy();
  });
});
